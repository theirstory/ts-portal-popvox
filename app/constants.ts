export const MIN_SCORE_THRESHOLD = 0.5;

export const PAGINATION_ITEMS_PER_PAGE = 10;

// Recordings listing is shown on a single scrollable page (no pagination),
// so we fetch all recordings in one request up to this cap.
export const STORIES_PAGE_SIZE = 1000;
